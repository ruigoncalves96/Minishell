#include "../../includes/minishell.h"

void    heredoc_executer(t_token *token, t_env *env, t_prompt_info prompt_info)
{
    (void)token;
    (void)env;
    (void)prompt_info;
    ft_printf("HEREDOC\n");
}
